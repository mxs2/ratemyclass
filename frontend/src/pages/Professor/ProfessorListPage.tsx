import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ProfessorListPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Professors</Title>
      <p>Professor list coming soon...</p>
    </div>
  );
};

export default ProfessorListPage;